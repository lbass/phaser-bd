'use strict'
import {FsnMask} from '../fsnbase';

class BdMask extends FsnMask {
  constructor(props) {
    super(props);
  }

  onDown(button) {
    // console.info(this.o_id);
    return;
  }
}

export default BdMask;
